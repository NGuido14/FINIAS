"""
Director tool definitions.

The Director doesn't expose itself as a tool — it IS the top-level agent.
This module exists for consistency with the agent template pattern
and can be used for any Director-specific utility functions.
"""
